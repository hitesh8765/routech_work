[pytest]
markers =
    ui: tests that drive the real browser UI
    api: tests that hit the API layer only
    parcel: parcel booking tests
    smoke: quick smoke-test subset
addopts = -ra --strict-markers
